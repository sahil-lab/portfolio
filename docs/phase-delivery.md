# Phase delivery and explicit completion checks

Updated 14 September 2026. This is a working handoff, not a declaration that every production-brief item is complete. The established playable remains usable while the kingdom expands.

Local preview for every phase: follow the root README (`npm ci`, `npm run build`, then `node node_modules/vinext/dist/cli.js start --port 3000`). Open http://127.0.0.1:3000/. Hosted preview: https://living-computer-kingdom.sahil-999.chatgpt.site/ (private; hosting revisions are confirmed separately).

## 1 — First playable: functional baseline verified

Workshop, animated courier and Packet Press, four distinct recipients, optional delivery journal, completion and explicit new round, CPU/RAM routes, enterable Component Studio, reactive project sequence, System Information and HTML alternatives work. Touch controls exist; physical-phone acceptance remains open.

Evidence: `performance-playtest.md`, `art-assets.md`, `courier-loop.md`, `project-exhibits.md`. Earlier production-browser route completed all four deliveries, completion, save/reload, explicit second round and project entry/exit. Regression tests continue to cover inventory conservation, duplicate recipients, save corruption/version checks, doorway routes and demonstration pause/reset. These earlier checks are retained evidence, not described as freshly repeated in full on 14 September.

## 2 — Complete kingdom: all seven behaviors connected; spatial scope still open

This pass connected previously unused district simulations to approachable world terminals and animated machinery. CPU queue/core activity, RAM shelves, GPU object/light, packet hops, index pruning, replica readiness and Kafka consumer progress share simulation state with their controls. Controls use native HTML fields and explain their simplifications. Movement is disabled while operating a panel; the simulation keeps running. Close returns to exploration.

Fresh production-browser checks (14 September, 1280×720, Codex Chromium, local production server): used the map arrival then walked to each of seven terminals. This is a terminal interaction route, not a fresh complete world traversal.

| District | Observed result |
| --- | --- |
| CPU | Four rapid submissions queued; all four completed across the available cores. |
| RAM | Allocated thought-1 at 0x00, inspected it, released it; address became free. |
| GPU | Selected torus, copper and cool lighting; the actual world object changed to a torus. |
| Network | Sent to RAM; displayed delivered at hop 3. |
| Database | Search 110 narrowed 16 → 7 → 3 → record 110, Pod manifest. |
| Kubernetes | Failed pod-a; service routed to ready pod-b; replacement pod-r1 restored 2/2 ready. |
| Kafka | Published to P1:0, advanced garden to 1 while archive stayed 0, then sought garden back to 0. |

Five focused new model tests protect FIFO execution, allocation capacity, network/index completion, ready-replica routing, partition ordering and independent offsets. Total suite: 26 passing tests.

Remaining: database is a ground-level demonstrator, not the required enormous subterranean room. CPU task elevators, RAM moving archive/treehouse scale, cable tunnels and some district-specific architecture remain abbreviated. Existing RAM ramp/balcony, sky lift and guarded bridge are usable. All seven district routes were walked in the preceding performance pass, but not every architectural ambition is implemented. Supplied-project expansion is blocked on actual repositories/descriptions; only labeled examples are currently available.

## 3 — Art and usability: substantial assets delivered; acceptance incomplete

Packet Press `.blend`, deterministic builder, source textures, named GLB nodes/clip and simplified collision are delivered and tested against the actual export. Original mural source and optimized wall texture, expressive code-authored courier/residents, original synthesized district music/cues and editable cinematic scene are present. See `art-assets.md` and `audio-cinematic.md`.

This pass removed the terminal backdrop blur so the machinery remains visible while its controls run, and gave controls cream surfaces, readable contrast and 44px minimum targets. Screenshot evidence is retained locally under `outputs/playtest/`.

Remaining: most individual characters/scenery are still code-authored rather than separate finished Blender/GLB assets. Physical phone and simultaneous-touch hardware tests have not occurred. Prior portrait/landscape emulation and pointer cancellation tests are not physical-device certification. Prior desktop 60 FPS target was unmet (best recorded short foreground sample 53.9 FPS); 30 FPS on a declared midrange phone remains unverified. New terminals require a fresh sustained foreground performance route before claiming a new budget result.

## 4 — Final package: reproducible handoff available; final acceptance open

Production build, 26 focused tests, actual exported GLB checks, source package and separate finished 30-second cinematic are available. `outputs/Kingdom-source.zip` includes application/UI/configuration, lockfile, scripts/tests/docs, public assets, source textures, Packet Press and cinematic Blender scenes. `scripts/package-source.py` validates archive CRCs and required entries. Dependencies and private credentials are excluded. Run the root README procedures after extraction.

Film: `outputs/cinematic/The-Living-Computer-Kingdom.mp4`, 1920×1080, 30 seconds, 24 FPS master with 12 authored poses/second, original AAC stereo. Full decode and authored-shot inspection are documented in `audio-cinematic.md`; it is a completed render, not merely a render script. The cinematic predates the new terminal integration and its staged railway shot is not evidence of live routing behavior.

Remaining final acceptance depends on the open spatial/art items, supplied portfolio facts, physical-device tests and measured performance target. No real résumé, achievement, architecture or source/demo link has been invented. Keep this checklist open until those checks have actually passed.

Final production checks: TypeScript and build passed. Complete client-assets estimate: 2,188,060 bytes raw / 860,335 bytes independently gzipped (includes lazy chunks; excludes HTML/HTTP overhead). This is below 10 MB, not an HTTP first-load capture. Portrait terminal controls were inspected at 390×844; the world is mostly covered by the panel on narrow screens, so close it to inspect the result.

Landscape 844×390 scrollable panel was also inspected. GPU select controls and Close measured at least 44px high; Close was 44×44px. Browser console returned no errors for this verification session. These are viewport emulations, not simultaneous physical touches.


